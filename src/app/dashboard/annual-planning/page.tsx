import AnnualPlanningClient from './client';

export const metadata = {
    title: 'التخطيط السنوي - لוח גנט | مدرستي',
};

export default function AnnualPlanningPage() {
    return <AnnualPlanningClient />;
}
